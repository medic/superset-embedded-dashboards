apiVersion: v1
kind: Service
metadata:
  name: {{ include "superset-embedded-dashboards.fullname" . }}
  labels:
    {{- include "superset-embedded-dashboards.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "superset-embedded-dashboards.selectorLabels" . | nindent 4 }}
